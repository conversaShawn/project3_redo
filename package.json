{
  "devDependencies": {
    "babel-core": "^6.26.3",
    "babel-loader": "^8.2.2",
    "babel-preset-es2015": "^6.24.1",
    "babel-preset-react": "^6.24.1",
    "fs-extra": "^9.1.0"
  },
  "dependencies": {
    "axios": "^0.21.1",
    "cypress": "^7.2.0",
    "debug": "^4.3.2",
    "kill-port-process": "^2.3.0",
    "react-file-base64": "^1.0.3"
  },
  "scripts": {
    "cy:open": "cypress open",
    "cy:client": "cypress open --env fileConfig=client",
    "cy:deployed": "cypress open --env fileConfig=deployed",
    "cy:server": "cypress open --env fileConfig=server"
  }
}
