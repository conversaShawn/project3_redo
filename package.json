{
  "devDependencies": {
    "cypress": "^7.0.1",
    "fs-extra": "^9.1.0"
  },
  "dependencies": {
    "axios": "^0.21.1",
    "react-file-base64": "^1.0.3"
  },
  "scripts": {
    "cy:open": "cypress open",
    "cy:client": "cypress open --env fileConfig=client",
    "cy:fullStack": "cypress open --env fileConfig=fullStack",
    "cy:server": "cypress open --env fileConfig=server"
  }
}
